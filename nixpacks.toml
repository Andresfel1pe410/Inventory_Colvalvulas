["phases.setup"]
nixPkgs = ["python311"]

["phases.install"]
cmds = [
  "python311 -m pip install --upgrade pip",
  "python311 -m pip install -r requirements.txt",
]

["phases.build"]
cmds = []

["start"]
cmd = "python311 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 4"

